services:
  - type: web
    name: edunotify
    runtime: node
    plan: free

    # Install npm packages and build React, caching Puppeteer in Render's build cache
    buildCommand: |
      export PUPPETEER_CACHE_DIR=/opt/render/project/puppeteer
      npm install
      npm run build

    # Start the Express server (serves built React + API)
    startCommand: node server.js

    envVars:
      - key: NODE_ENV
        value: production
      - key: PUPPETEER_CACHE_DIR
        value: /opt/render/project/puppeteer
      - key: BREVO_KEY
        sync: false            # Set this in Render Dashboard -> Environment
      - key: SENDER_EMAIL
        sync: false            # Set this in Render Dashboard -> Environment
      - key: FRONTEND_URL
        sync: false            # e.g. https://edunotify.onrender.com
